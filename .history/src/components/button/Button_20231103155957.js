import React from "react";

const Button = ({
  style = "",
  onClick = () => {},
  type = "button",
  ...rest
}) => {
  return <button
  type={type}
  onClick={}
  ></button>;
};

export default Button;
