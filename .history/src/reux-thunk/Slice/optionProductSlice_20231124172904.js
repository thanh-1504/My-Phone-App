import { createSlice } from "@reduxjs/toolkit";

const optionProductSlice = createSlice({
    name: 'option',
    initialState: {dataOption: []},
    reducers: {}
})
export default optionProductSlice.